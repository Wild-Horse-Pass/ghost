//! `wraith` — Wraith Wallet CLI.
//!
//! Thin client that speaks JSON-RPC to a running `wraithd`. Phase 0 stub:
//! prints a banner and exits. Real subcommands arrive once the IPC surface lands.

fn main() {
    println!("wraith CLI (phase 0 stub)");
}
