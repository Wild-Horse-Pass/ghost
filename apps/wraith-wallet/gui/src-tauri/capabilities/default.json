{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Wraith Wallet GUI — only the bare minimum needed to invoke our own commands from the static frontend.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
