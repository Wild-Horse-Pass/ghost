# Ghost Pool Test Script Environment Variables
# Copy this file to .env and fill in values before running test scripts.
#
# Usage: source scripts/.env && ./scripts/test-deployment.sh

# Bitcoin Core / Ghost Core RPC credentials
GHOST_RPC_USER=ghostrpc
GHOST_RPC_PASSWORD=
