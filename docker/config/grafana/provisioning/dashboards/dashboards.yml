apiVersion: 1

providers:
  - name: 'Ghost Pool Dashboards'
    orgId: 1
    folder: 'Bitcoin Ghost'
    folderUid: 'ghost'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
